# Prefer the shared environment at the repo root:
#   pip install -r ../requirements.txt
#
# Kept for convenience when working only inside a1/.
torch==2.12.0
nltk==3.9.4
transformers==5.10.1
numpy==2.4.6
datasets>=4.0.0
accelerate>=1.1.0
